# Kangi — 앱으로 내놓기까지

**현재 상태** — `index.html` 하나짜리 정적 웹페이지. Vercel에 올라가 있고, 브라우저로 열면 잘 돌아간다.
**앱이 되려면** 「설치된다 · 오프라인에서 된다 · 데이터가 안 날아간다」 셋이 필요하다. 지금은 셋 다 없다.

아래는 그 셋을 채우는 순서다. 위에서부터 하면 각 단계마다 쓸 만한 결과가 나온다.

---

## 지금 당장 문제가 되는 것 세 가지

| # | 문제 | 지금 | 앱이면 |
|---|---|---|---|
| 1 | **발음이 네트워크에 묶여 있다** | 이 PC에 깔린 일본어 음성이 `Google 日本語` 하나뿐인데 네트워크 음성 | 지하철에서 안 됨 — 단어장 앱의 핵심 기능이 죽는다 |
| 2 | **책갈피가 사라질 수 있다** | `localStorage` | iOS 사파리는 7일 안 쓰면 지운다(ITP). 백업 수단도 없다 |
| 3 | **설치가 안 된다** | manifest·service worker 둘 다 없음 | 홈 화면 아이콘도, 오프라인도 없음 |

**2번이 제일 급하다.** 기능이 아니라 **사용자가 쌓은 것이 날아가는** 문제라서다.
앱 작업을 안 하더라도 백업/복원은 지금 넣는 게 맞다.

---

## 1단계 — 데이터를 지킨다 (앱과 무관하게 먼저)

- [ ] **저장 키를 한군데로 모은다.** 지금 흩어져 있다 — `kangi-bookmarks` · `kangi-mode` · `kangi-anchor-word` · `kangi-anchor-grammar` · `kangi-rate` · `kangi-toc-shut` · `kangi-night`
- [ ] **내보내기 / 가져오기.** JSON 한 덩어리를 파일로 저장하고 다시 읽는다. 계정 없이 기기를 옮길 수 있는 유일한 길
- [ ] **`localStorage` → IndexedDB 검토.** ITP 삭제 대상에서 벗어나고 용량 제한(5MB)도 풀린다.
      다만 지금 저장하는 양이 작아서 **급하지 않다.** 내보내기가 있으면 당분간 버틴다
- [ ] **저장 실패를 조용히 삼키지 않는다.** 지금 `store.set`은 `catch(e){}`다. 사파리 프라이빗 모드에서 통째로 무시된다

## 2단계 — PWA (제일 싼 길, 스토어 없이 설치됨)

- [ ] **`manifest.webmanifest`** — `name` · `short_name` · `display:"standalone"` · `theme_color:"#17795a"` · `background_color` · `lang:"ko"` · `orientation`
- [ ] **아이콘 파일.** 지금 파비콘은 인라인 SVG data URI라 **manifest에서 못 쓴다.** PNG가 필요하다
  - `192×192` · `512×512` · `512×512 maskable`(안전 영역 안쪽에 그림)
  - iOS용 `apple-touch-icon` 180×180
- [ ] **서비스 워커.** 파일이 하나라 precache가 단순하다. **캐시 버전을 올리는 규칙**을 같이 정해야 한다 —
      안 그러면 업데이트해도 옛 화면이 계속 뜬다
- [ ] **iOS 대응** — `apple-mobile-web-app-capable` · status bar style. iOS는 manifest만으로 스플래시가 안 나온다
- [ ] **설치 안내 UI** — Android/데스크톱은 `beforeinstallprompt`, iOS는 「공유 → 홈 화면에 추가」 문구를 직접 띄워야 한다
- [ ] **`.vercelignore` 조정** — 지금은 `index.html` 하나만 올린다. manifest·아이콘·sw.js가 올라가게 열어줘야 한다
- [ ] **safe-area 재확인** — `viewport-fit=cover`와 `env(safe-area-inset-bottom)`은 이미 있다. standalone 모드에서 상단바도 확인

## 3단계 — 오프라인 발음 (제일 무겁고, 제일 값이 큰 것)

브라우저 TTS는 오프라인을 보장하지 않는다. 앱으로 낼 거면 **여기서 답을 정해야 한다.**

- [ ] **선택 A — 오디오를 미리 굽는다.** 단어 1048 + 예문 1681 + 활용 카드 읽기 ≈ **3천 개 남짓**
  - 장점: 확실히 오프라인, 음질 일정
  - 단점: 용량. opus 기준 개당 8~15KB로 잡아도 **30~45MB.** 지금 앱(572KB)의 60배
  - 다운로드를 나눠 받게 하는 설계가 필요하다 (주제별로 받기)
  - **TTS 서비스 라이선스를 반드시 확인할 것** — 생성물 재배포를 금지하는 곳이 있다
- [ ] **선택 B — 네이티브 TTS를 쓴다.** Capacitor로 감싸고 OS TTS 호출. 용량 0, 대신 4단계가 전제
- [ ] **선택 C — 그냥 온라인 기능으로 둔다.** 오프라인일 때 발음 버튼을 흐리게 하고 안내. 정직하지만 앱의 값이 준다
- [ ] 어느 쪽이든 **음성이 없는 기기에서의 안내 문구**는 지금도 있으니 그대로 살린다

## 4단계 — 스토어 배포 (필요할 때만)

PWA로 충분하면 **이 단계는 건너뛰어도 된다.** 스토어에 올려야 할 이유가 생겼을 때만 한다.

- [ ] **포장 방식 결정** — Android는 TWA(PWA를 그대로 감쌈)가 제일 싸다. iOS까지 하려면 Capacitor
- [ ] **Android** — 패키지명 · 서명 키 보관 · Play Console 등록비 $25(1회) · 데이터 보안 양식
- [ ] **iOS** — Apple Developer $99/년 · Xcode 빌드
  - ⚠ **웹뷰만 감싼 앱은 반려될 수 있다** (App Store Review Guideline 4.2 Minimum Functionality).
    오프라인 · 알림 · 위젯처럼 **웹으로 안 되는 것이 하나는 있어야** 안전하다. 3단계 선택 B가 그 답이 된다
- [ ] **개인정보처리방침 URL** — 데이터를 아무것도 안 모아도 **문서 자체는 필요하다.** 「수집하지 않음」을 명시
- [ ] **앱 이름 확인** — "Kangi"가 스토어에서 겹치는지, 상표 문제가 없는지

## 5단계 — 앱다워지는 것 (심사 통과에도 도움)

- [ ] **복습 알림** — 로컬 푸시. 「오늘 20단어」. 앱의 존재 이유가 되는 기능
- [ ] **진도** — 어디까지 읽었나. 지금은 「읽던 위치」뿐이고 **본 단어/안 본 단어 구분이 없다** (`IDEAS.md`에 있음)
- [ ] **글자 크기 조절** — 지면이 화면 높이에 맞춰 쪽을 나누므로 폰트를 바꾸면 쪽 나눔도 다시 계산된다. 구조는 이미 된다
- [ ] **접근성** — 스크린리더 라벨, 대비. 아이콘 버튼에 `aria-label`은 이미 붙어 있다
- [ ] **시스템 다크모드 연동** — 지금은 의도적으로 `prefers-color-scheme`을 안 본다(3번 결정). 앱에서는 따라가는 편이 자연스러울 수 있다 — **다시 정할 것**

## 6단계 — 운영

- [ ] **버전 표시** — 화면 어딘가에 빌드 버전. 사용자가 「업데이트됐는지」 알 수 있어야 한다
- [ ] **에러 로깅** — 지금 0개. 최소한 `window.onerror`로 콘솔에라도 남긴다
- [ ] **콘텐츠 라이선스 명시** — 단어·예문을 직접 썼다면 그 사실을. 사전에서 가져온 것이 섞였는지 확인
- [ ] **메타 설명 갱신** — `<meta name="description">`이 아직 「단어 902개와 문법 강의 10편」이다 (실제 1048단어 · 17편)

---

## 순서 요약

```
1단계 데이터 지키기 ──→ 앱과 무관하게 지금 해야 함 (반나절)
        │
2단계 PWA ─────────→ 여기까지만 해도 「설치되는 앱」이 된다 (하루)
        │
3단계 오프라인 발음 ─→ 선택 A/B/C 결정이 먼저 (설계 반나절 + 실행은 선택에 따라)
        │
4단계 스토어 ───────→ 정말 필요할 때만. iOS는 3단계 B가 사실상 전제
        │
5·6단계 ──────────→ 계속
```

**제일 먼저 정해야 할 것은 3단계의 A/B/C다.** 이 선택이 4단계 가능 여부를 결정한다.

---

## 안 하기로 한 것 (지금은)

- **계정 · 서버 동기화** — 백엔드가 생기는 순간 「의존성 0」이 깨진다. 내보내기/가져오기로 버틴다
- **빌드 도구 도입** — 서비스 워커와 manifest는 파일 두어 개 추가일 뿐이다. 번들러까지는 아직 필요 없다
- **퀴즈 · 플래시카드** — 처음부터 안 하기로 한 것 (`README.md` 「지켜온 원칙」)
