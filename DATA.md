# Kangi — 콘텐츠가 커진 뒤의 관리

N4를 닫은 시점의 상태를 재고, 데이터 쪽과 사용자 쪽에서 각각 무엇을 해야 하는지 적는다.

---

## 0. 지금 상태 — 잰 숫자

| | |
|---|---|
| `index.html` | **856KB** — 그중 `DATA` 배열이 **87%**, 코드+CSS가 13% |
| 섹션 | 54 (단어 28 · 문법 26) |
| 항목 | **2,089** (단어 모드 1,076 · 문법 모드 1,013) |
| 문법 편당 | 평균 39장 · 최대 51장 |
| 레벨 | 단어 N5 23 · N4 5 / 문법 N5 12 · N4 14 |

**856KB는 아직 문제가 아니다.** 반복이 많은 텍스트라 gzip이 잘 먹고, 요청 한 번이면 끝이며 캐시된다.
성능이 걸리는 것은 `ROADMAP-N1.md`에 적은 대로 **N3 중반(4~6MB)**부터다.

**지금 실제로 아픈 곳은 크기가 아니라 두 가지다.**

1. **작성 흐름** — 2만 줄짜리 HTML 안에서 강의를 추가한다. 매번 「DATA만 빼내 → 검사 → 다시 끼워넣기」를 하고 있다
2. **검색 구멍** — 설명 카드 369장과 표지 55장, **전체의 20%가 검색에 아예 안 들어간다**

---

# 1부 — 데이터 쪽 (개발)

## 1-1. 검증을 코드로 고정한다 ★ 제일 먼저

지금까지 편을 얹을 때마다 임시 스크립트로 확인해 왔다. **그걸 파일로 만들어 저장소에 둔다.**

- [ ] **`tools/check.js`** — `node tools/check.js` 한 줄로 전부 검사
  - key 중복 (`cover|si|j` · `note|si|j` · `pair|a|b` · `adjc|w|r|tag` · `conj|w|say|tag`)
  - 예문 루비 정렬 실패 (`alignRuby`가 null을 뱉는 문장)
  - 필수 칸 누락 (`w·r·k·p·e·ek·eo`, 예문 2개면 `e2·ek2·eo2`)
  - **일본어 칸에 한글·로마자가 섞였는지** — 실제로 두 번 저질렀다(`food を残して`, `준비が必要`)
  - `rows`에 `"→"`를 직접 적었는지 (README에 적힌 그 실수)
  - `**강조**`가 `note.p` 밖에 쓰였는지 (파서가 거기만 있다)
  - `<script>` 블록 구문 검사 (`node --check`)

**이유** — 위 항목은 전부 **실제로 한 번 이상 터진 것들**이다. 사람이 매번 기억할 일이 아니다.

## 1-2. 표지의 카드 수를 손으로 쓰지 않는다

`{cover:true, … meta:"설명 16장 · 활용 18개 · 짝 8쌍"}` — 이 숫자를 손으로 적고 있다.
**㉑·㉕·⑱ 세 번 어긋나서 고쳤다.** 사람이 셀 일이 아니다.

- [ ] `meta`를 안 적으면 `coverHTML`이 **섹션의 실제 카드 수를 세어 채우게** 한다
- [ ] 지금처럼 직접 적고 싶으면 적되, `check.js`가 실제 수와 다르면 잡아낸다

## 1-3. `tag`를 세 카드 타입에 일관되게 둔다

`conj`와 `adjc`에는 `tag`를 열었는데(⑯편) **`pair`에는 없다.**
그래서 ㉒편에서 「開いています ↔ 開けてあります」가 ⑪편과 부딪혔을 때 **내용을 바꿔서 피했다.**
이번엔 결과가 좋았지만(같은 규칙을 다른 동사로 한 번 더 보게 됐다), **다음에도 운이 좋으리라는 보장은 없다.**

- [ ] `pair`에도 `tag` — key를 `pair|a.w|b.w|tag`로
- [ ] 세 타입의 key 규칙을 한 함수로 모아 `check.js`와 공유

## 1-4. 데이터를 파일에서 분리한다 — 다만 이유를 정확히

**성능 때문이 아니다. 작성 때문이다.** 856KB는 아직 빠르다.

- [ ] `data/words-n5.js` · `data/words-n4.js` · `data/grammar-n5.js` · `data/grammar-n4.js`
- [ ] **일반 `<script src>`로 읽는다. ES 모듈은 쓰지 않는다** —
      모듈은 `file://`에서 CORS로 막혀서 **더블클릭으로 안 열린다.** 일반 스크립트는 열린다
- [ ] 각 파일이 전역 배열에 밀어 넣고, `index.html`이 이어 붙여 `DATA`를 만든다
- [ ] `.vercelignore`를 고쳐 `data/`가 올라가게 한다

**무엇을 잃나** — 「파일 하나」라는 문구. **무엇을 지키나** — 빌드 0 · 의존성 0 · 더블클릭으로 열림.
`README.md`「지켜온 원칙」의 **핵심은 지켜진다.** 문구만 「폴더 하나」로 고쳐 적으면 된다.

**언제 하나** — N3을 시작하기 전. 데이터가 지금의 세 배가 된 뒤에 옮기면 훨씬 비싸다.

## 1-5. 쪽 나눔 성능을 지금 재둔다

`paginate()`는 **현재 모드의 모든 항목 높이를 실측**한다. 지금 모드당 약 1,000장이다.

- [ ] 더미 3,000장을 넣고 재분할 시간을 잰다. **추측하지 말고 숫자를 본다**
- [ ] 느리면 「읽는 곳 앞뒤만 재고 나머지는 필요할 때」로 바꾼다
- [ ] 19번에서 고친 여백 계산은 그대로 살린다

## 1-6. 상호 참조를 데이터로 만든다

본문에 「⑨편에서 배운」이 **수십 군데** 있다. 지금은 그냥 글자다.

- [ ] 편 번호를 `{ref:9}` 같은 데이터로 두고 렌더러가 글자와 링크를 만든다
- [ ] **누르면 그 편으로 넘어간다** (사용자 쪽 이득, 2-3 참고)
- [ ] `ROADMAP-N1.md`에 적은 **번호 표기 변경**(㊿에서 원문자가 끝난다)이 이걸 안 하면 전수 수정이 된다

---

# 2부 — 사용자 쪽 (색인과 구분)

## 2-1. 검색에 설명 카드를 넣는다 ★★ 제일 싸고 제일 큰 것

**지금 `SEARCH`는 `if(w.note || w.cover) return;`으로 설명 카드와 표지를 통째로 건너뛴다.**

| | 항목 | 검색됨 |
|---|---|---|
| 단어·활용·짝·표 | 1,665 | ○ |
| **설명 카드** | **369** | **✗** |
| **표지** | **55** | **✗** |

**전체의 20%가 안 찾힌다.** 그리고 그 20%가 **문형의 이름이 적혀 있는 유일한 곳**이다.

실제로 확인해 보면 — 「**ことになる**」, 「**いらっしゃる**」로 검색하면 **아무것도 안 나온다.**
㉔편과 ㉖편이 그것을 한 장씩 통째로 설명하고 있는데도 그렇다.
카드에 적힌 것은 활용된 꼴(`ことになりました` · `いらっしゃいます`)뿐이고, **사전형은 설명 본문에만 있기 때문**이다.

- [ ] `note`의 `t`(제목) · `p`(본문) · `rows`를 검색 `body`에 넣는다
- [ ] `cover`의 `t` · `sub` · `bullets`도 넣는다
- [ ] **`conj`의 `steps` 글자도 빠져 있다** — 카드에 크게 찍혀 있는데 검색이 안 된다. 같이 넣는다
- [ ] 결과 목록에서 설명 카드는 「설명」 꼬리표를 달아 단어와 구분한다

## 2-2. 문법 색인 — 문형 이름으로 찾는 목록 ★

목차는 **편 제목**만 보여준다. 「〜ておく가 어디 있더라」를 답해주지 못한다.
26편 · 문법 항목 180개쯤 되면 **교과서 뒤에 붙는 색인**이 필요하다.

- [ ] 문형 이름 → 편 번호의 평평한 목록. **오십음순** 정렬
- [ ] 데이터로 둔다 — 섹션에 `index:["〜ておく","〜てみる","〜てしまう"]`
- [ ] 서랍에 **네 번째 탭**으로 넣거나, 목차 위에 「색인」 토글
- [ ] 한 문형이 여러 편에 나오면 전부 보여준다 — 「〜ておく … ⑪ 맛보기 · ㉒ 본편」.
      **이 책은 같은 것을 여러 편에서 다시 다루는 것이 방침**이라(22번) 이 표시가 특히 값이 있다

## 2-3. 상호 참조를 누를 수 있게

본문의 「⑨편」·「⑯편」이 수십 군데다. **지금은 눌러도 아무 일도 없다.**
독자는 그때마다 서랍을 열어 목차에서 그 편을 찾아야 한다.

- [ ] 편 참조를 링크로 (1-6의 데이터화가 전제)
- [ ] 돌아오기 — 「원래 보던 쪽으로」 버튼. 없으면 링크가 오히려 불편하다

## 2-4. 진도 — 본 것과 안 본 것

2,089장이 되면 **어디까지 봤는지**가 「읽던 위치」 하나로는 부족하다.
`IDEAS.md`에 이미 적혀 있고, **지금이 그 시점**이다.

- [ ] 쪽을 지나가면 그 항목을 「본 것」으로 표시 (자동)
- [ ] 목차에 섹션별 진도 — 「음식 · 요리 ② · 28개 중 12」
- [ ] **저장이 커진다.** 지금 `localStorage`에 책갈피 몇십 개인데, 진도는 2,089개다.
      `APP.md` 1단계(내보내기 · IndexedDB 검토)와 **같이 해야 한다**

## 2-5. 카드 종류로 걸러 보기 — 복습 수단

퀴즈는 안 만들기로 했다(지켜온 원칙). 그런데 **이미 있는 것을 걸러 보는 것**만으로 복습이 된다.

- [ ] 「짝 카드만」 — 대조가 본체인 자리만 훑는다. 189쌍
- [ ] 「표만」 — 활용표만 몰아본다. 127장
- [ ] 「책갈피만」 — 이미 있는 책갈피를 쪽으로 엮어 본다
- [ ] **필터를 걸면 쪽이 다시 나뉜다.** `paginate`가 리스트를 받으므로 구조는 이미 된다

## 2-6. 목차가 곧 한 화면을 넘는다

7번·16번에서 두 번 겪었다. 지금 54개 · 2단이고, N3이 들어오면 **5단 · 100개**가 된다.

- [ ] 지금 레벨만 보기 (기본값을 「진행 중인 레벨」로)
- [ ] 섹션 검색 — 목차 안에서 제목 필터
- [ ] 레벨 묶음을 기본으로 접어두기 (지금은 기본 펼침)

---

## 3. 순서 — 무엇부터

```
지금 (N4 완주 직후)
 ├─ 2-1  검색에 설명 카드 넣기 ──── 반나절 · 효과 제일 큼 · 위험 0
 ├─ 1-1  tools/check.js ────────── 반나절 · 앞으로 계속 값을 함
 └─ 1-2  표지 카드 수 자동 생성 ─── 한 시간

N3 시작 전 (구조를 건드리는 것들)
 ├─ 1-4  데이터 파일 분리 ───────── 「파일 하나」 문구를 포기하는 결정
 ├─ 1-6  상호 참조 데이터화 ─────── 번호 표기 변경의 전제
 ├─ 1-5  paginate 성능 측정 ────── 먼저 재고 나서 판단
 └─ 1-3  pair에 tag
        + ROADMAP-N1.md 의 결정 셋

그 다음 (사용자 기능)
 ├─ 2-2  문법 색인
 ├─ 2-3  상호 참조 링크
 ├─ 2-4  진도  ← APP.md 1단계(저장소)와 묶어서
 ├─ 2-5  카드 종류 필터
 └─ 2-6  목차 정리
```

**첫 줄 둘(2-1 · 1-1)은 오늘 해도 되는 것**이다. 구조를 안 건드리고, 되돌리기도 쉽다.
나머지는 **N3을 시작하기 전에 몰아서** 하는 편이 낫다 — 데이터가 세 배가 된 뒤에 옮기면 전부 비싸진다.

---

## 4. 안 하기로 한 것

- **DB · 서버** — 읽기 전용 콘텐츠다. 파일로 충분하다
- **번들러 도입** — 스크립트 태그 네 개면 되는 일에 도구를 들이지 않는다
- **검색 라이브러리** — 2,089개 선형 훑기는 지금도 즉시다. 1만 개가 넘으면 그때 다시 본다
- **퀴즈 · 플래시카드** — 처음부터 안 하기로 한 것. 2-5의 필터가 그 자리를 대신한다
